# Never commit personal/business contact info or local notes into this public repo
*Company Info*
*.env*
.DS_Store
*:Zone.Identifier

# Raw/unoptimized source photos - keep the optimized copies in images/ instead
/*.jpg
/*.JPG
/*.png
/*.PNG
